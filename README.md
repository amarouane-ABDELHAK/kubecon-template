# Quick Start
```bash
tofu init
tofu apply --auto-approve

```
Visit https://localhost.local/argocd for argocd
Visit https://localhost.local for main app

```text
Add localhost.local to /etc/hosts

```
